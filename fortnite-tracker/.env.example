# Supabase (obligatorio para API en Vercel)
SUPABASE_URL=
SUPABASE_KEY=

# Resend (opcional: sin esto no se envían mails)
RESEND_API_KEY=
# Valores por defecto si no están en tabla `config`:
RESEND_FROM=onboarding@resend.dev
ALERT_EMAIL=

# Panel admin en /admin (opcional). Si está vacío, /api/admin/* responde 404.
ADMIN_SECRET=

# URL canónica del sitio (opcional). Si no se define, se usa el host de cada request.
PUBLIC_API_BASE=https://tu-dominio.vercel.app
